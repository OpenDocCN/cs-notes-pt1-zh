# 022：递推关系 T(n)=2T(n-1)+1 求解 🧮

![](img/fe53d4ef7e0c0164b6b991107806a4e8_1.png)

在本节课中，我们将学习如何求解一个特定的递推关系：`T(n) = 2T(n-1) + 1`。我们将通过两种方法——递归树法和反向代入法——来推导其时间复杂度，并最终理解其属于指数级复杂度 `O(2^n)`。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_3.png)

## 从算法到递推关系

![](img/fe53d4ef7e0c0164b6b991107806a4e8_5.png)

首先，我们从一个示例算法开始，并推导出其时间复杂度的递推关系。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_7.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_9.png)

考虑一个函数，它执行一次打印操作（耗时1个单位时间），然后递归调用自身两次，每次调用的输入规模减1。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_11.png)

因此，该算法的时间函数 `T(n)` 可以表示为：
**T(n) = 2T(n-1) + 1**

![](img/fe53d4ef7e0c0164b6b991107806a4e8_13.png)

其完整的递推关系定义如下：
*   **T(n) = 2T(n-1) + 1**，当 n > 0 时。
*   **T(0) = 1**，这是递归的基本情况（常数时间）。

## 方法一：递归树法求解 🌳

上一节我们得到了递推关系，本节中我们使用递归树法来直观地求解它。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_15.png)

递归树展示了函数调用层层展开的过程。对于 `T(n) = 2T(n-1) + 1`：
*   根节点代价为 **1**（对应公式中的 `+1`）。
*   它产生两个子问题，每个子问题的规模是 `n-1`，即 `T(n-1)`。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_17.png)

以下是展开步骤：
1.  第一层（根）：代价为 **1**。
2.  第二层：有 **2** 个节点，每个节点代价为 **1**，总代价为 **2 * 1 = 2**。
3.  第三层：有 **4** 个节点，每个节点代价为 **1**，总代价为 **4 * 1 = 4**。
4.  以此类推，直到第 **k** 层，问题规模变为 0 (`T(0)`)。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_19.png)

当 `n - k = 0`，即 `k = n` 时，递归停止。此时，树的总层数为 `n+1`（从第0层到第n层）。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_21.png)

现在，我们来计算整棵树的总代价。总代价是每一层代价之和。

以下是各层代价的序列：
*   第0层（根）：1 = 2^0
*   第1层：2 = 2^1
*   第2层：4 = 2^2
*   ...
*   第n层：2^n

因此，总时间 `T(n)` 是一个等比数列（Geometric Progression, GP）的和：
**T(n) = 1 + 2 + 2^2 + 2^3 + ... + 2^n**

![](img/fe53d4ef7e0c0164b6b991107806a4e8_23.png)

等比数列求和公式为：**S = a * (r^(k+1) - 1) / (r - 1)**，其中 `a` 是首项，`r` 是公比，`k` 是最大指数。
在此数列中，`a = 1`，`r = 2`，`k = n`。代入公式：
**T(n) = 1 * (2^(n+1) - 1) / (2 - 1) = 2^(n+1) - 1**

![](img/fe53d4ef7e0c0164b6b991107806a4e8_25.png)

所以，**T(n) = 2^(n+1) - 1**。当 `n` 很大时，主导项是 `2^(n+1)`。因此，时间复杂度为 **O(2^n)**。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_26.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_27.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_29.png)

## 方法二：反向代入法求解 🔄

![](img/fe53d4ef7e0c0164b6b991107806a4e8_31.png)

除了递归树法，我们还可以使用反向代入法来求解这个递推关系。这种方法通过反复将递推式右侧的 `T` 项用其定义进行展开。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_33.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_34.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_36.png)

我们从原始递推式开始：
**(1) T(n) = 2T(n-1) + 1**

![](img/fe53d4ef7e0c0164b6b991107806a4e8_38.png)

第一次代入：将等式右边的 `T(n-1)` 用 `T(n-2)` 表示（即 `T(n-1) = 2T(n-2) + 1`）。
**T(n) = 2 * [2T(n-2) + 1] + 1 = 2^2 * T(n-2) + 2 + 1**

![](img/fe53d4ef7e0c0164b6b991107806a4e8_40.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_41.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_43.png)

第二次代入：将 `T(n-2)` 用 `T(n-3)` 表示。
**T(n) = 2^2 * [2T(n-3) + 1] + 2 + 1 = 2^3 * T(n-3) + 2^2 + 2 + 1**

![](img/fe53d4ef7e0c0164b6b991107806a4e8_44.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_46.png)

观察规律，在进行第 `k` 次代入后，我们得到：
**(4) T(n) = 2^k * T(n-k) + (2^{k-1} + 2^{k-2} + ... + 2^2 + 2 + 1)**

我们一直代入，直到递归到达基本情况，即 `T(0)`。此时 `n - k = 0`，所以 `k = n`。同时，我们知道 `T(0) = 1`。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_48.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_49.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_51.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_52.png)

将 `k = n` 和 `T(0) = 1` 代入公式 (4)：
**T(n) = 2^n * T(0) + (2^{n-1} + 2^{n-2} + ... + 2 + 1)**
**T(n) = 2^n * 1 + (2^{n-1} + 2^{n-2} + ... + 2 + 1)**

![](img/fe53d4ef7e0c0164b6b991107806a4e8_54.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_56.png)

括号内是一个等比数列求和（首项1，公比2，共n项），其和为 `2^n - 1`。
因此：
**T(n) = 2^n + (2^n - 1) = 2^{n+1} - 1**

结论与方法一完全一致：**T(n) = 2^{n+1} - 1**，时间复杂度为 **O(2^n)**。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_58.png)

## 总结 📝

![](img/fe53d4ef7e0c0164b6b991107806a4e8_60.png)

本节课中我们一起学习了如何求解递推关系 `T(n) = 2T(n-1) + 1`。
*   我们首先从算法逻辑推导出该递推式。
*   接着，使用**递归树法**直观地展示了递归展开过程，并通过等比数列求和得到解 `T(n) = 2^{n+1} - 1`。
*   然后，使用**反向代入法**进行了严谨的代数推导，得到了相同的结果。
*   最终，我们确定该算法的时间复杂度属于指数阶 **O(2^n)**。这意味着随着输入规模 `n` 的增大，运行时间将变得非常长，效率很低。

![](img/fe53d4ef7e0c0164b6b991107806a4e8_62.png)

![](img/fe53d4ef7e0c0164b6b991107806a4e8_64.png)

在接下来的课程中，我们将学习用于解决递减函数递推关系的**主定理（Master Theorem for Decreasing Functions）**，它提供了一种更快捷的判定方法。